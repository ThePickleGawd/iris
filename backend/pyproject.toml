[project]
name = "iris-backend"
version = "0.2.0"
description = "Iris server — sessions, agent, widgets"
requires-python = ">=3.12"
dependencies = [
    "flask==3.1.0",
    "openai",
    "httpx",
    "manim",
]

